<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rename File</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
  <div class="max-w-2xl w-full bg-white p-6 rounded-lg shadow-lg">
    
    <!-- Rename File Form -->
    <h1 class="text-2xl font-bold mb-4 text-center">Rename File</h1>
    
    <form id="renameForm" class="space-y-4" action="/edit" method="post">
      <!-- Show previous file name (disabled input for display) -->
      <label class="block text-gray-700 font-semibold">Current File Name</label>
      <input type="text" value="<%= filename  %>" class="w-full p-2 border border-gray-300 rounded bg-gray-200" name="previous" >

      <!-- Input for new file name -->
      <label class="block text-gray-700 font-semibold">New File Name</label>
      <input type="text" name="new" placeholder="Enter New File Name" required class="w-full p-2 border border-gray-300 rounded">

      <!-- Update Button -->
      <button type="submit" class="w-full bg-yellow-500 text-white p-2 rounded hover:bg-blue-600">Update Name</button>
    </form>

    <!-- Back Button -->
    <div class="mt-4 text-center">
      <a href="/" class="text-blue-500 hover:underline">⬅ Go Back</a>
    </div>
  </div>
</body>
</html>
